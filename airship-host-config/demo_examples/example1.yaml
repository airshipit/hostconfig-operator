apiVersion: hostconfig.airshipit.org/v1alpha1
kind: HostConfig
metadata:
  name: example1
spec:
  # Add fields here
  message: "Its a big world"
  host_groups:
    - "master"
    - "worker"
  execution_order: false
