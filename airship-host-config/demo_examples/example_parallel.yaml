apiVersion: hostconfig.airshipit.org/v1alpha1
kind: HostConfig
metadata:
  name: example7
spec:
  # Add fields here
  host_groups:
   - name: "kubernetes.io/role"
     values:
      - "master"
      - "worker"
  max_hosts_parallel: 2
